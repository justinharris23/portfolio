export default function About(props) {
  return <h2> Welcome {props.userName}</h2>;
}

// INSTALL react-router-dom in our root directory
// INDEX: import browserrouter and wrap up App in it in index.js
// IMPORT: importing Link and Route(s) into our components
// INITIALIZE: initializing the Link and Routes
